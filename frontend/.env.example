# 充电桩管理系统 - 环境配置模板
# 复制此文件为 .env 并根据实际情况修改

# ===== 数据库配置 =====
DB_HOST=localhost
DB_PORT=3306
DB_NAME=charging_station
DB_USER=charging
DB_PASSWORD=charging123
DB_ROOT_PASSWORD=root

# ===== 后端应用配置 =====
BACKEND_PORT=8080
SPRING_PROFILE=production

# ===== 前端应用配置 =====
FRONTEND_PORT=80
FRONTEND_SSL_PORT=443
NGINX_HOST=localhost
NGINX_PORT=80

# ===== Java 虚拟机配置 =====
# -Xmx: 最大堆内存
# -Xms: 初始堆内存
# -XX:+UseG1GC: 使用 G1 垃圾收集器
JAVA_OPTS=-Xmx1G -Xms512M -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -Dfile.encoding=UTF-8

# ===== 应用特定配置 =====
# JWT 密钥（必须修改！）
JWT_SECRET=your-super-secret-jwt-key-please-change-this-in-production

# 允许的来源域名（CORS）
ALLOWED_ORIGINS=http://localhost,http://localhost:3000,http://localhost:80

# 应用名称
APP_NAME=Charging Station Management System
APP_VERSION=1.0.0

# ===== 日志配置 =====
LOG_LEVEL=INFO

# ===== SSL/HTTPS 配置 =====
# 如果使用自签名证书
SSL_CERT_PATH=/etc/nginx/certs/server.crt
SSL_KEY_PATH=/etc/nginx/certs/server.key

# ===== 邮件配置（可选）=====
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USER=noreply@example.com
MAIL_PASSWORD=your-email-password
MAIL_FROM=noreply@example.com

# ===== 文件上传配置 =====
UPLOAD_DIR=/var/uploads
MAX_UPLOAD_SIZE=10485760  # 10MB

# ===== 定时任务配置 =====
SCHEDULER_ENABLED=true

# ===== 监控和指标 =====
METRICS_ENABLED=true
HEALTH_CHECK_ENABLED=true
